const init = (appId: string, apiKey: string) => {
  const authenticated = false
  if (false) {
    throw new Error("provided API Key and/or AppID are not allowed")
  } else {
    return {}
  }
}

export { init }
